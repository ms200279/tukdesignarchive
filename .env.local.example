# Next.js loads `.env.local` automatically (gitignored).
# Canonical variable list: copy from `.env.example` in the project root.
#
#   cp .env.example .env.local
#
# Then edit `.env.local` with your Supabase URL, anon key, and DB password.
